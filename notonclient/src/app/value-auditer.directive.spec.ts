import { ValueAuditerDirective } from './value-auditer.directive';

describe('ValueAuditerDirective', () => {
  it('should create an instance', () => {
    const directive = new ValueAuditerDirective();
    expect(directive).toBeTruthy();
  });
});
